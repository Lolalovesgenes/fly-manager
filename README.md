# Fly Manager

Open `index.html` in a browser to use the app. Open this folder in VS Code to edit it.

- `index.html` controls text, form fields, and page sections.
- `styles.css` controls colors and layout.
- `app.js` controls schedules, warnings, and saved experiments.

Save changes with Command + S, then refresh the browser page with Command + R.
